#include <stdio.h>
/**
 * main - point d'entrée.
 * Return: toujours 0.
 */
int main(void)
{
int nombre;
for (nombre = 0; nombre < 10; nombre++)
putchar(nomber + '0');
for (nombre = 0; nombre < 6; nombre++)
putchar(nombre + 'a');
putchar('\n');
return (0);
}
